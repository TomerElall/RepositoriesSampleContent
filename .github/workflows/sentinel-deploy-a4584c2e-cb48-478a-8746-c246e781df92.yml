name: Deploy Content to sentinel-jamescoliz-DSPM-04 [a4584c2e-cb48-478a-8746-c246e781df92]
# Note: This workflow will deploy everything in the root directory.
# To deploy content only from a specific path (for example SentinelContent):
#   1. Add the target path to the "paths" property like such
#    paths:
#    - 'SentinelContent/**'
#    - '!.github/workflows/**'
#    - '.github/workflows/sentinel-deploy-a4584c2e-cb48-478a-8746-c246e781df92.yml'
#   2. Append the path to the directory environment variable below
#       directory: '${{ github.workspace }}/SentinelContent'

on:
  push:
    branches: [ main-sentinel-deployment-sentinel-deployment ]
    paths:
    - '**'
    - '!.github/workflows/**'  # this filter prevents other workflow changes from triggering this workflow
    - '.github/workflows/sentinel-deploy-a4584c2e-cb48-478a-8746-c246e781df92.yml'

jobs:
  deploy-content:
    runs-on: windows-latest
    env:
      resourceGroupName: 'sentinel-rg-jamescoliz-DSPM-04'
      workspaceName: 'sentinel-jamescoliz-DSPM-04'
      workspaceId: 'f27bee96-0c3f-48a4-b63e-b6c95328d6bf'
      directory: '${{ github.workspace }}'
      cloudEnv: 'AzureCloud'
      contentTypes: 'CustomDetection'
      branch: 'main-sentinel-deployment-sentinel-deployment'
      sourceControlId: 'a4584c2e-cb48-478a-8746-c246e781df92'
      rootDirectory: '${{ github.workspace }}'
      githubAuthToken: ${{ secrets.GITHUB_TOKEN }}
      smartDeployment: 'true'
    permissions:
      contents: write
      id-token: write # Require write permission to Fetch an OIDC token.

    steps:
    - name: Login to Azure (Attempt 1)
      continue-on-error: true
      id: login1
      uses: azure/login@v2
      with:
        client-id: ${{ secrets.AZURE_SENTINEL_CLIENTID_a4584c2ecb48478a8746c246e781df92 }}
        tenant-id: ${{ secrets.AZURE_SENTINEL_TENANTID_a4584c2ecb48478a8746c246e781df92 }}
        subscription-id: ${{ secrets.AZURE_SENTINEL_SUBSCRIPTIONID_a4584c2ecb48478a8746c246e781df92 }}
        environment: 'AzureCloud'
        audience: api://AzureADTokenExchange
        enable-AzPSSession: true

    - name: Wait 30 seconds if login attempt 1 failed
      if: ${{ steps.login1.outcome=='failure' }}
      run: powershell Start-Sleep -s 30

    - name: Login to Azure (Attempt 2)
      continue-on-error: true
      id: login2
      uses: azure/login@v2
      if: ${{ steps.login1.outcome=='failure' }}
      with:
        client-id: ${{ secrets.AZURE_SENTINEL_CLIENTID_a4584c2ecb48478a8746c246e781df92 }}
        tenant-id: ${{ secrets.AZURE_SENTINEL_TENANTID_a4584c2ecb48478a8746c246e781df92 }}
        subscription-id: ${{ secrets.AZURE_SENTINEL_SUBSCRIPTIONID_a4584c2ecb48478a8746c246e781df92 }}
        environment: 'AzureCloud'
        audience: api://AzureADTokenExchange
        enable-AzPSSession: true

    - name: Wait 30 seconds if login attempt 2 failed
      if: ${{ steps.login2.outcome=='failure' }}
      run: powershell Start-Sleep -s 30

    - name: Login to Azure (Attempt 3)
      continue-on-error: false
      id: login3
      uses: azure/login@v2
      if: ${{ steps.login2.outcome=='failure'  }}
      with:
        client-id: ${{ secrets.AZURE_SENTINEL_CLIENTID_a4584c2ecb48478a8746c246e781df92 }}
        tenant-id: ${{ secrets.AZURE_SENTINEL_TENANTID_a4584c2ecb48478a8746c246e781df92 }}
        subscription-id: ${{ secrets.AZURE_SENTINEL_SUBSCRIPTIONID_a4584c2ecb48478a8746c246e781df92 }}
        environment: 'AzureCloud'
        audience: api://AzureADTokenExchange
        enable-AzPSSession: true

    - name: Checkout
      uses: actions/checkout@v3

    - name: Deploy Content to Microsoft Sentinel
      uses: azure/powershell@v2
      with:
        azPSVersion: 'latest'
        inlineScript: |
          ${{ github.workspace }}//.github/workflows/azure-sentinel-deploy-a4584c2e-cb48-478a-8746-c246e781df92.ps1